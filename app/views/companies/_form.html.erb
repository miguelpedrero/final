<%= form_for(@new_company, remote:true) do |f| %>
  <% if @new_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_company.errors.count, "error") %> prohibited this @new_company from being saved:</h2>

      <ul>
      <% @new_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
